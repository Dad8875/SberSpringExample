server:
  port: 9090
  servlet:
    context-path: "/api/v1"

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sber_db
    username: postgres
    password: ABHczkfGZogjGH1ya4ly
#    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update


